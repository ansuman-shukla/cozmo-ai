[pytest]
addopts = --import-mode=importlib
testpaths =
    contracts/tests
    backend/tests
    agent/tests
    knowledge/tests
    tests
markers =
    unit: fast, isolated tests
    integration: tests that cross a service or package boundary
    e2e: full-stack synthetic call-flow tests
    load: concurrency and capacity tests
asyncio_mode = auto
